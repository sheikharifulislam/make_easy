import React from "react";

const PropsComponent = ({ children }) => {
    return (
        <div>
            <p>I don't know what is this</p>
            {children}
        </div>
    );
};

export default PropsComponent;
